---
title: zkVM symbol reference
description: Complete reference for the extern symbols that zkVM hosts must implement to integrate the Zesu guest program.
keywords:
  [
    zesu,
    zkVM,
    symbols,
    extern,
    read_input,
    write_output,
    accelerators,
    precompiles,
    rv64im,
  ]
---

`zesu.rv64im.o` leaves all platform-specific behaviour as unresolved extern symbol
references.
A zkVM host satisfies these references by providing a host object that implements each
symbol using platform-native circuits or software fallbacks.

The symbols fall into three categories: IO, runtime, and accelerators.

## IO symbols

IO symbols are the only channel between Zesu and the zkVM host at runtime.
Zesu calls `read_input` once to receive the `StatelessInput` bundle and `write_output`
once to emit the 41-byte public commitment.

| Symbol       | Signature                        | Description                                    |
|--------------|----------------------------------|------------------------------------------------|
| `read_input` | `(*[*]const u8, *usize) void`    | Fill pointer and length with zkVM private input |
| `write_output` | `([*]const u8, usize) void`    | Emit public output bytes                       |

## Runtime symbols

Runtime symbols provide execution environment support for logging, process termination,
and memory management.

| Symbol          | Signature                        | Description                                      |
|-----------------|----------------------------------|--------------------------------------------------|
| `zkvm_log`      | `(u8, [*]const u8, usize) void`  | Log a message at the given level                 |
| `zkvm_exit`     | `(i32) noreturn`                 | Terminate execution with exit code               |
| `ZKVM_HEAP_POS` | `usize` (var)                    | Bump heap cursor; the allocator advances this    |
| `ZKVM_HEAP_TOP` | `usize` (var)                    | Heap upper bound; the allocator checks against this |

## Accelerator symbols

Accelerator symbols delegate EVM precompile operations to zkVM-native circuits.
All accelerator symbols return `i32`: `0` for success, `-1` for failure.

| Symbol                        | Precompile | Description                                          |
|-------------------------------|------------|------------------------------------------------------|
| `zkvm_keccak256`              |            | Keccak-256 hash                                      |
| `zkvm_sha256`                 | `0x02`     | SHA-256 hash                                         |
| `zkvm_secp256k1_ecrecover`    | `0x01`     | secp256k1 signature recovery                         |
| `zkvm_secp256k1_verify`       |            | secp256k1 signature verification                     |
| `zkvm_ripemd160`              | `0x03`     | RIPEMD-160 hash                                      |
| `zkvm_modexp`                 | `0x05`     | Modular exponentiation (EIP-198)                     |
| `zkvm_bn254_g1_add`           | `0x06`     | BN254 G1 point addition (EIP-196)                    |
| `zkvm_bn254_g1_mul`           | `0x07`     | BN254 G1 scalar multiplication (EIP-196)             |
| `zkvm_bn254_pairing`          | `0x08`     | BN254 pairing check (EIP-197)                        |
| `zkvm_blake2f`                | `0x09`     | BLAKE2f compression (EIP-152)                        |
| `zkvm_kzg_point_eval`         | `0x0a`     | KZG point evaluation (EIP-4844)                      |
| `zkvm_bls12_g1_add`           | `0x0b`     | BLS12-381 G1 addition (EIP-2537)                     |
| `zkvm_bls12_g1_msm`           | `0x0c`     | BLS12-381 G1 multi-scalar multiplication (EIP-2537)  |
| `zkvm_bls12_g2_add`           | `0x0d`     | BLS12-381 G2 addition (EIP-2537)                     |
| `zkvm_bls12_g2_msm`           | `0x0e`     | BLS12-381 G2 multi-scalar multiplication (EIP-2537)  |
| `zkvm_bls12_pairing`          | `0x0f`     | BLS12-381 pairing check (EIP-2537)                   |
| `zkvm_bls12_map_fp_to_g1`     | `0x10`     | BLS12-381 Fp to G1 map (EIP-2537)                    |
| `zkvm_bls12_map_fp2_to_g2`    | `0x11`     | BLS12-381 Fp2 to G2 map (EIP-2537)                   |
| `zkvm_secp256r1_verify`       | `0x100`    | P-256 signature verification (EIP-7212)              |

The precompile column shows the EVM precompile address that each accelerator corresponds
to, where applicable.
Symbols without a precompile address (`zkvm_keccak256` and `zkvm_secp256k1_verify`) are
used internally by the EVM interpreter rather than dispatched through the precompile
mechanism.

## Next steps

- [Obtain the guest program](../get-started/get-guest-program.mdx): how to download the
  pre-built artifact and link it against a host object.
- [Architecture](../concepts/architecture.mdx): why the symbol contract is structured this
  way and how it fits into the proving pipeline.
